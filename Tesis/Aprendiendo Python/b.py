#!/usr/bin/python
        
        
        #for j in range(1,480,1): 
	  #for i in range(1,640,1):
	     #c=cv.Get2D(image,j,i)
	     #if c[0] == 255: 
	       #old_coord  = new_coord
	       #new_coord += [(i,j)]
	       #if (old_coord - 5 < new_coord < old_coord + 5)
	           #averx = averx + i 
	           #avery = averx + j
	           #count = count + 1
	       #else 
	           #p = p + 1
	           #new_coord += [(i,j)]

(x, y, z) = (3.0 , 4 , 7.0) 
print (z/x)

for j in range(1,10,1):
    exec('var%d = j'%j)
    
var3= [[1,0,0],[0,1,0],[0,0,1]]

print var1,var2,var3,var4